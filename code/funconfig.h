#ifndef FUNCONFIG_H
#define FUNCONFIG_H

// Use the default HSI (Internal clock) running at 48MHz
#define FUNCONF_USE_PLL 1

// Enables printf over the debug wire (super handy for debugging later!)
#define FUNCONF_USE_DEBUGPRINTF 1

#endif